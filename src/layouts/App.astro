---
import Base from './Base.astro';

interface Props {
  title?: string;
}

const { title = 'Verocity' } = Astro.props;
const path = Astro.url.pathname.replace(/\/+$/, '') || '/';
const links = [
  { href: '/app', label: 'Home' },
  { href: '/app/calendar', label: 'Calendar' },
  { href: '/app/stats', label: 'Stats' },
  { href: '/app/coach', label: 'Coach' },
  { href: '/app/plan', label: 'Plan' },
  { href: '/app/library', label: 'Library' },
  { href: '/app/shares', label: 'Share' },
];
const isActive = (href: string) => (href === '/app' ? path === '/app' : path.startsWith(href));
---

<Base title={title}>
  <a href="#main" class="skip-link">Skip to content</a>
  <header class="sticky top-0 z-50 border-b border-border bg-bg/90 backdrop-blur-md">
    <div class="mx-auto flex h-16 max-w-3xl items-center px-6">
      <button
        id="nav-toggle"
        type="button"
        aria-expanded="false"
        aria-controls="nav-menu"
        class="flex items-center gap-2.5 text-fg"
      >
        <span class="flex flex-col gap-[5px]" aria-hidden="true">
          <span class="block h-[2px] w-5 bg-fg"></span>
          <span class="block h-[2px] w-5 bg-fg"></span>
          <span class="block h-[2px] w-5 bg-fg"></span>
        </span>
        <span class="font-display text-lg font-semibold uppercase tracking-tight">Verocity</span>
      </button>
    </div>
    <nav id="nav-menu" hidden class="border-t border-border bg-bg" aria-label="Primary">
      <div class="mx-auto max-w-3xl px-6 py-2">
        {
          links.map((l) => (
            <a
              href={l.href}
              aria-current={isActive(l.href) ? 'page' : undefined}
              class:list={[
                'block py-2 text-sm uppercase tracking-wider transition-colors duration-150 hover:text-fg',
                isActive(l.href) ? 'text-fg' : 'text-muted',
              ]}
            >
              {l.label}
            </a>
          ))
        }
      </div>
    </nav>
  </header>
  <main id="main"><slot /></main>
</Base>

<script>
  const toggle = document.getElementById('nav-toggle');
  const menu = document.getElementById('nav-menu');
  if (toggle && menu) {
    let open = false;
    const setOpen = (next: boolean) => {
      open = next;
      toggle.setAttribute('aria-expanded', String(next));
      menu.hidden = !next;
    };
    toggle.addEventListener('click', (e) => {
      e.stopPropagation();
      setOpen(!open);
    });
    document.addEventListener('click', (e) => {
      const target = e.target as Node;
      if (open && !menu.contains(target) && !toggle.contains(target)) setOpen(false);
    });
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape') setOpen(false);
    });
  }
</script>
