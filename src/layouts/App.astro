---
import Base from './Base.astro';

interface Props {
  title?: string;
}

const { title = 'Verocity' } = Astro.props;
---

<Base title={title}>
  <header class="border-b border-border">
    <div class="mx-auto flex max-w-3xl items-center justify-between px-6 py-4">
      <a href="/app" class="font-display text-lg uppercase tracking-[0.2em] text-fg">Verocity</a>
      <nav class="flex gap-5 text-[0.7rem] uppercase tracking-wider text-muted">
        <a href="/app" class="hover:text-fg">Home</a>
      </nav>
    </div>
  </header>
  <slot />
</Base>
